const { NODE_ENV } = process.env;
const { GEOAPIFY_API_KEY } = process.env;
const { MAPBOX_KEY } = process.env;
const { ALPHAVENTAGE_KEY } = process.env;
const { OPENEXCHANGERATE_KEY } = process.env;
const { APP_CONTACT_ADDRESS } = process.env;
const { APP_CONTACT_EMAIL } = process.env;
const { GEOAPIFY_BASE_URL } = process.env;
const { OPENEXCHANGERATE_URL } = process.env;
const { ALPHAVENTAGE_URL } = process.env;

export {
  ALPHAVENTAGE_KEY,
  ALPHAVENTAGE_URL,
  APP_CONTACT_ADDRESS,
  APP_CONTACT_EMAIL,
  GEOAPIFY_API_KEY,
  GEOAPIFY_BASE_URL,
  MAPBOX_KEY,
  NODE_ENV, OPENEXCHANGERATE_KEY,
  OPENEXCHANGERATE_URL,
};
